scrape_configs:
  - job_name: 'inventory-service'
    scrape_interval: 10s
    metrics_path: '/q/metrics'
    static_configs:
      - targets: ['host.docker.internal:8083'] # Use host.docker.internal instead of localhost in Prometheus targets so the container can reach your host-running Quarkus app.
